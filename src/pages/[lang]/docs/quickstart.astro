---
import DocQuickstart from "../../../components/docs/DocQuickstart.astro";
import { langs, defaultLang, type Lang } from "../../../i18n/config";
export function getStaticPaths() {
  return langs.filter((l) => l !== defaultLang).map((l) => ({ params: { lang: l } }));
}
---
<DocQuickstart />
